/// <reference types="react" />
import './donut.scss';
import { DonutProps } from '../../types';
declare const Donut: ({ size, strokeWidth, className, children, color, style, trackColor, trackStrokeWidth, styleTrack, styleIndicator, animate, linecap, indicatorStrokeWidth, indicatorColor, }: DonutProps) => JSX.Element;
export default Donut;
