import { CSSProperties } from 'react';
import './donut-value.scss';
import { DonutProps } from '../../types';
declare type children = string | number;
declare type Props = Pick<DonutProps, 'style' | 'className'> & Partial<{
    children: children;
    symbol: string;
    styleContainer: CSSProperties;
    styleSymbol: CSSProperties;
    presentValue: (value: number | string) => any;
    symbolPosition: 'bottom-right' | 'bottom-left' | 'top-left' | 'top-right';
    showValue: boolean;
}>;
declare const DonutValue: ({ children, style, styleSymbol, styleContainer, presentValue, symbol, symbolPosition, className, showValue, }: Props) => JSX.Element;
export default DonutValue;
