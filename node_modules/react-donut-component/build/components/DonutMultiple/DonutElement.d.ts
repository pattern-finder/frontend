/// <reference types="react" />
import { DonutProps } from '../../types';
declare type Props = DonutProps & {
    ratio: number;
    increaseFilled: (itemRatio: number) => any;
    cx?: number;
    cy?: number;
    dashArray: number;
    radius?: number;
    startAngle: number;
    filled: number;
    angle: number;
    offset: number;
    itemRatio: number;
    name: string;
};
declare const DonutElement: ({ color, cx, cy, radius, strokeWidth, dashArray, startAngle, angle, offset, animate, }: Partial<Props>) => JSX.Element;
export default DonutElement;
