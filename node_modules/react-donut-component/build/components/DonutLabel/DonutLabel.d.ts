import { CSSProperties, ReactNode } from 'react';
import './donut-label.scss';
declare type Props = {
    children?: ReactNode;
    className?: string;
    style?: CSSProperties;
    multiple?: boolean;
};
declare const DonutLabel: ({ children, className, style, multiple }: Props) => JSX.Element;
export default DonutLabel;
